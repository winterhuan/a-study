# lance

## 编译问题

```shell
cd lance/rust/lance-encoding && rm protos && ln -s ../../protos protos && cd ~/workspace/lanceStudy


cd lance/rust/lance-encoding && ln -s ../../protos protos && cd ~/workspace/lanceStudy
```
